[Service]
# Keep ~/.local packages from shadowing the deployment environment.  In
# particular, an incompatible user-site NumPy prevented executor.py from
# processing otherwise valid SDF submissions.
Environment=PYTHONNOUSERSITE=1
